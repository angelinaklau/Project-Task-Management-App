{% extends 'layouts/master.twig' %}
{% block title %}Daftar User{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-3">
  <h2>Daftar User</h2>
  <a href="/users/create" class="btn btn-primary">
    <i class="fas fa-plus"></i> Tambah User
  </a>
</div>


<table  class="table table-striped table-bordered datatable">
  <thead class="table-dark">
    <tr>
      <th>No</th>
      <th>Nama</th>
      <th>Email</th>
      <th>Role</th>
      <th>Aksi</th>
    </tr>
  </thead>
  <tbody>
    {% for list in lists %}
      <tr>
        <td>{{ loop.index }}</td>
        <td>{{ list.name }}</td>
        <td>{{ list.email }}</td>
        <td>{{ list.role }}</td>
        <td>
            <a href="/users/{{ list.id }}/edit" class="btn btn-sm btn-warning me-1">
                <i class="fas fa-edit"></i>
            </a>
            <form method="post" action="/users/{{ list.id }}/delete" class="d-inline" onsubmit="return confirm('Yakin ingin menghapus data ini?');">
                <button type="submit" class="btn btn-sm btn-danger">
                <i class="fas fa-trash-alt"></i>
                </button>
            </form>
        </td>
      </tr>
    {% endfor %}
  </tbody>
</table>

{% endblock %}
